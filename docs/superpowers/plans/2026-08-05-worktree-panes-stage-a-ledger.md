# SDD ledger — plan: docs/superpowers/plans/2026-08-05-worktree-panes-stage-a.md

Worktree: .claude/worktrees/worktree-panes-stage-a (branch feature/worktree-panes-stage-a, off master 3d8f91c)

## Pre-flight notes

- `internal/gitworktree/` is ALREADY IMPLEMENTED and was seeded into this
  worktree as untracked files before Task 1. It is the plan's "Already Done"
  section. It needs committing but not writing.
- **Plan conflict found and resolved by the controller:** Task 4's render code
  calls `m.worktreeVisibleRows()` and reads `m.worktreeScroll`, but
  `worktreeVisibleRows` is not defined until Task 5. Resolution: Task 4 defines
  a simple `worktreeVisibleRows` returning a fixed cap; Task 5 replaces its body
  with the shared-budget version and subtracts it inside `sessionVisibleRows`.
  Communicated in both dispatches.
- Task 5's test references `setupChromeRows`. The real constant in
  `internal/tui/sessions.go` is a function-local `const surroundingRows = 26`.
  Resolution: Task 5 introduces a package-level `setupChromeRows = 26` and both
  `sessionVisibleRows` and `worktreeVisibleRows` derive from it, so the test and
  the implementation cannot drift.

## Progress
Task 1: implemented (commit 8687f71, base 3d8f91c) — IPC contract + internal/gitworktree brought into git. Review dispatched.
Task 1: complete (commits 3d8f91c..8687f71, review clean — spec OK, quality approved)
Task 1: minor (deferred): the two new Msg constants carry a 2-line comment the brief did not specify. Consistent with every other constant group in protocol.go; kept.
Task 1: minor (deferred): commit body is empty for a 446-line/5-file commit. Not rewriting published-to-branch history for it.
Task 1: note — reviewer independently ran `GOOS=windows go vet ./internal/gitworktree/...` (passes). Worth repeating for any later task touching *_windows.go, since dev.sh runs in a Linux container and never compiles those files.
Task 2: implemented (commit 1095ccb, base 8687f71) — daemon handler + single-flight slot. Review dispatched.
Task 2: complete (commits 8687f71..1095ccb, review clean — spec OK, quality approved, zero findings)
Task 3: implemented (commit 67d2c0e, base 1095ccb) — worktreeState + request/apply/timeout + Update wiring. Review dispatched.
Task 3: complete (commits 1095ccb..67d2c0e, review clean — spec OK, quality approved)
Task 3: minor (deferred): decode arm placed beside MsgKubeCtxResp rather than MsgGitReposResp as the brief said. Behaviourally identical; reviewer judged the discovery-RPC grouping arguably better. Kept.
Task 3: ruling — the reqGen/nextReqGen comment extensions are maintenance forced by the requested field addition, not scope creep. Leaving them would make an existing comment false.
Task 4: implemented (commit 24a86dd, base 67d2c0e) — DONE_WITH_CONCERNS: field insertion shifted cursor indices, forcing edits to 7 pre-existing test cases in sessions_test.go + setup_dialog_test.go (outside the brief's file list). Review dispatched with that as primary focus.
Task 4: complete (commits 67d2c0e..24a86dd, review clean — spec OK, quality approved)
Task 4: ruling — the 7 edited pre-existing tests are CORRECT UPDATES, not weakening. Reviewer hand-traced setupFieldKind for every plugin shape; one subtest actually GAINED an assertion. sessionKeyModel self-verifies its cursor resolves to "session", so its ~10 dependents cannot silently drift.
Task 4: minor (deferred): stale comment at setup_dialog_test.go:1756 says "Continue" where index 1 is now the worktree field. Test still valid (asserts only that CWD is blurred).
Task 4: CARRY INTO TASK 7 — .claude/rules/tui-dialogs.md still documents field order as "CWD -> kube -> toggles -> session -> Continue", now stale. Must gain the worktree field.
Task 5: PLAN DEFECT found by implementer, confirmed by controller. The brief's test asserts total <= h-setupChromeRows, but total can never drop below worktreeListMinRows+sessionListMinRows (1+4=5), so the assertion is unsatisfiable for any h < 31. Three of the four swept heights (12/16/24) were below it.
Task 5: ruling — test-only fix, clamp the bound to the combined floor. Rejected the alternative (retune the height table) because it would stop exercising the floor region entirely, so a future floor change would go uncaught. Implementation functions were correct as specified and are unchanged.
Task 5: COST RECORDED — adding the worktree list raises the dialog's minimum usable height by exactly one row (30 -> 31). Below that the dialog does not fit, which is accepted, not prevented (same precedent as historyMinRows / minTermWidth).
Task 5: implemented (commit 207dbf9, base 24a86dd) — shared height budget + clamped test per ruling + 2 collateral subtest fixes in TestSessionVisibleRows_ShrinksOnShortTerminal. Review dispatched.
Task 5: CONTROLLER RULING WAS WRONG — correcting it. I ruled "test-only fix, implementation correct as specified". Empirical sweep (h=26..45) shows the shared-budget invariant is VIOLATED at h=29..35, seven consecutive heights. At h=33: wt=6 + sess=4 = 10 against a budget of 7. That is the [Continue]-off-screen bug this task exists to prevent.
Task 5: root cause — worktreeVisibleRows claims greedily (up to 6) without reserving sessionListMinRows(4), whose floor does not shrink. The brief's four heights {12,16,24,40} straddle the failing band without touching it, so the test passes over a real defect.
Task 5: fix loop round 1 dispatched — implementation change (worktree reserves the session floor) AND test change (sweep the contiguous band, not scattered points).
Task 5: rev-task5 returned "approved, no findings" on the DEFECTIVE diff (report crossed with my stop message). It hand-recomputed h=12/16/24/40/44/60 — every point the test names plus two more — and never checked 29..35. It even verified "would a revert be caught?" affirmatively for two constructed reverts while the live defect sat unexamined between its sample points.
Task 5: PROCESS LESSON — two independent parties (controller + reviewer), both reasoning carefully, missed a seven-height contiguous failure band because both reasoned about the heights the TEST NAMES. Sampled-point verification reproduces the test's own blind spot. For any threshold/banded arithmetic, sweep the contiguous range; do not sample it, and do not accept a hand-recomputation of the sample points as verification.
Task 5: this approval is NOT accepted. Fix round 1 remains in flight.
Task 5: rev-task5 answered the focused questions and CONFIRMED the fix direction by full hand-sweep 26..50. (a) Reserving sessionListMinRows inside worktreeVisibleRows eliminates the band everywhere rather than moving it — during the ramp h-26-wt(h) collapses to a constant 5, so total equals budget exactly at every height. (b) The asymmetry is correct: worktree's own floor is self-guaranteed (derived from height alone, never reads session), so the only missing piece was worktree reserving what it owes session; the smaller-cap/smaller-floor field is the right one to yield. (c) Keep an ENUMERATIVE sweep, not a closed-form property — a formula relocates the same fragile derivation into the test, whereas a sweep sidesteps needing to know the breakpoints (31/37/44) at all.
Task 5: additions accepted — widen the sweep top bound to 48 for margin past the both-capped transition, and add TestWorktreeVisibleRows_ShrinksOnShortTerminal to pin worktree's own floor/ramp/cap regions in isolation (currently only the combined test touches it).
Task 5: fix round 1 result — commit fa53d04 "fix(tui): reserve the session floor in the worktree row budget". Implementer independently re-derived the failure band by hand before touching code, and produced red-then-green evidence by stashing only the dialog.go fix (same 7-height failure list, worst h=33 at 3 rows over) then restoring it.
Task 5: CONTROLLER VERIFIED INDEPENDENTLY — own probe swept h=20..60 (wider than the implementer's 26..45 and the reviewer's 26..50): zero overflows. Band eliminated, not relocated.
Task 5: fix round 2 dispatched for two outstanding items from my addendum (arrived after the work was done): widen sweep to h<=48, and add TestWorktreeVisibleRows_ShrinksOnShortTerminal to pin worktree's clamp in isolation. Neither is a defect — the shipped behaviour is correct — they are coverage.
Task 5: fix round 2 result — commit 5340d35 (sweep widened to 48, TestWorktreeVisibleRows_ShrinksOnShortTerminal added, mutation-tested against the round-1 stub then restored byte-identically).
Task 5: implementer CORRECTED the controller's mechanism description before it entered a code comment — during worktree's ramp the constant is 1+sessionListMinRows (=5), session sitting ONE ROW ABOVE its floor via its default branch, not AT the floor. Verified by controller probe: h=32..37 all show sess=5, floor=4. My description was imprecise; the committed comment is correct.
Task 5: CONTROLLER RE-VERIFIED after round 2 — swept h=20..80, zero overflows.
Task 5: controller correction — I reported the two additions as "not landed"; that grep ran against the pre-5340d35 state. Both are present at the committed head (sweep h<=48 at setup_dialog_test.go:2190, isolated test at :2231). Timing artifact on my side, not an implementer miss.
Task 5: complete (commits 24a86dd..5340d35 across 3 commits: 207dbf9 defective, fa53d04 behavioural fix, 5340d35 coverage). Behaviour independently verified by controller sweeps at h=20..60 and h=20..80, zero overflows. Scoped re-review dispatched in parallel for test non-vacuity and comment accuracy.
Task 5: scoped re-review CLEAN — all 3 findings ADDRESSED, no new breakage, no scope creep, both commit messages compliant.
Task 5: CORRECTS A CONTROLLER ASSUMPTION — I requested TestWorktreeVisibleRows_ShrinksOnShortTerminal partly as insurance against the original overflow. It is NOT. Re-reviewer hand-computed the round-1 formula in isolation: it yields a perfectly well-formed floor/ramp/cap function (monotonic, <=1-row steps, floor and cap both reached in range), so the isolated test passes under the buggy version. The defect is purely an INTERACTION between worktreeVisibleRows and sessionVisibleRows; only the combined sweep guards it. The isolated test still earns its place — it catches gross breakage of that function's own contract — but it adds no defence against the bug that motivated it.
Task 5: minor (deferred): setup_dialog_test.go:2201 uses a bare literal 1 where worktreeListMinRows would read better. Carried from the brief's own text, untouched by the fix.
Task 5: FINAL — complete (24a86dd..5340d35), 2 fix rounds, behaviour verified by controller sweeps at h=20..60 and h=20..80.
Task 6: implemented (commit f523b72, base 5340d35) — DONE_WITH_CONCERNS.
Task 6: FOURTH PLAN DEFECT — my plan said to call onSetupCWDChanged "from every site that commits a new browsed directory in handleSetupCWDKey". No such site exists: browser navigation is ASYNC via browseTo/browseUp -> applyBrowseResponse. Implemented literally, the primary "browse to another repo then submit" path would carry a stale worktree. Implementer found the real commit site and wired it there plus ensureSessionScan. Accepted.
Task 6: deviation accepted — reused historyWindow rather than adding worktreeWindow (the plan's own comment invited exactly this) and wired it into renderSetupWorktreeField as well, closing a stale-scroll gap the plan's snippets missed.
Task 6: RESIDUAL BUG confirmed by controller, fix round 1 dispatched. showRootsList (dialog.go:2856) sets cwdBrowseDir="" without resetting the worktree choice, so setupSpawnDir() keeps returning the stale worktree and the pane spawns in a repo the user navigated away from. Reachable on Windows by pressing "up" from a drive root (browseUp -> showRootsList). Implementer flagged it rather than expanding scope, which was the right call.
Task 6: fix round 1 result — commit d9176d9, reset added inside showRootsList gated on m.dialog==dialogCreatePaneSetup (same pattern as applyBrowseResponse). Two tests added: one driven through the real browseUp path, one asserting the project dialog is untouched. Task review dispatched over BOTH commits (5340d35..d9176d9), since Task 6 went straight from DONE_WITH_CONCERNS into the fix round without a task review.
Task 6: review verdict SPEC ❌ / NOT APPROVED — one Critical, verified independently by controller.
Task 6: CRITICAL — enterSetupOrSplit (dialog.go:2513) is the SOLE entry point into the setup dialog and resets cwdBrowseDir/repoScan/recentCandidates/kube/session but NOT selectedWorktree/worktreeCursor/worktreeScroll/worktrees (grep: zero occurrences). So: create a pane in repo A's worktree, press Ctrl+N again, git-discovery pre-fills repo B via a bare cwdBrowseDir assignment, user never focuses the worktree field, Continue -> setupSpawnDir() returns repo A's stale worktree and the pane spawns there while every visible field says repo B. Reachable in the single most ordinary flow, no special navigation.
Task 6: this is the THIRD instance of the same bug class found by a different route (applyBrowseResponse, showRootsList, now enterSetupOrSplit). Missed by the brief, by two implementer self-review passes, and by me. Found only because the review was asked to ENUMERATE every cwdBrowseDir assignment rather than spot-check. Same lesson as Task 5's sampled heights: checking the instances you think of reproduces your own blind spot.
Task 6: fix round 2 dispatched.
Task 6: fix rounds 2+3 complete (708d7f5 critical re-entry reset, 33c644f drop the dead plugin param — signature now onSetupCWDChanged(dir string), 0 nil passers, 8 call sites).
Task 6: CONTROLLER EXHAUSTIVE CHECK — traced every production write to cwdBrowseDir and every caller chain, not just the remembered sites. dialog.go: 1607 (teardown, covered by enterSetupOrSplit on next entry), 2869 (showRootsList, guarded reset follows), 2926 (applyBrowseListing — single non-test caller browse_client.go:182 -> applyBrowseDir -> single non-test caller dialog.go:2717 applyBrowseResponse, which resets on browseFilled), 3847 (inside onSetupCWDChanged itself). projectdialog.go 583/599 are project-dialog only and separated by the m.dialog guard. No fourth instance constructible.
Task 6: scoped re-review CLEAN — all 4 findings ADDRESSED, no new breakage, no scope creep, all 3 commits compliant.
Task 6: re-reviewer proved the invariant from the STRONGER direction — selectedWorktree has exactly TWO production writers (handleSetupWorktreeKey's Enter case, and onSetupCWDChanged's reset). That closes the question independent of how many browse paths exist, where my own enumeration only showed every known browse path resets. Also confirmed exactly one production writer of m.dialog=dialogCreatePaneSetup (dialog.go:2612), so enterSetupOrSplit really is the sole entry point.
Task 6: minor (deferred): round-3 commit body says "the two handleSetupPickKey sites"; there are three (syncSelection closure + two Enter branches). Code is correct, prose undercounts.
Task 6: complete (commits 5340d35..33c644f, 4 commits, 3 fix rounds — one Critical found and closed).
Task 7: implemented (commit 6e6d633, base 33c644f) — features.md subsection, tui-dialogs.md field-order fix, CHANGELOG entry. docs-size passed. Review dispatched.
Task 7: complete (commit 6e6d633, review clean — spec OK, quality approved, zero findings). Reviewer independently confirmed the quoted UI strings match the render code character-for-character and that gitworktree.Add has no production caller, so "Quil does not create worktrees yet" is accurate.
ALL 7 TASKS COMPLETE. Branch feature/worktree-panes-stage-a = 12 commits off master. Full suite green, vet clean.
Final whole-branch review dispatched over master..HEAD.
MERGE RISK (controller, measured not inferred): `git merge-tree` dry-run of fix/sidebar-attention-indicators against feature/worktree-panes-stage-a reports exactly ONE conflict — CHANGELOG.md, both branches inserting under ## [Unreleased]. docs/features.md is touched by both but merges cleanly (different regions). ZERO Go-file overlap: the PR touches sidebar.go/palette.go/ctxmenu.go, the feature touches dialog.go/model.go/sessions.go/recent_client.go. Resolution is to keep both changelog entries; whichever lands second owns it.
FINAL WHOLE-BRANCH REVIEW: MERGE. No Critical, no Important. Fourth instance of the stale-state class: NO (independently re-derived — 2 writers of selectedWorktree, 1 writer of m.dialog=dialogCreatePaneSetup). gitworktree.Add unreferenced; no WorktreeSpec/CreatePanePayload.Worktree/[git] config — stage B is genuinely absent.
FINAL REVIEW CORRECTED THE CONTROLLER on deferred minor #5: the bare literal 1 at setup_dialog_test.go:2201 is BETTER than worktreeListMinRows. "< 1" pins "at least one row is always drawn"; tracking the constant would make the assertion follow a floor raised above what the terminal holds — the historyMinRows failure its own neighbour warns about. My instinct to "fix" it was wrong.
FINAL REVIEW L2 — TASK 5's PREMISE WAS PARTLY WRONG. Only ONE setup-dialog field expands at a time (cursor == fieldIdx, a single int), so worktree and session lists are MUTUALLY EXCLUSIVE and the operative constraint is MAX, not SUM. The shared-budget arithmetic is correct about a quantity that is not the real constraint. Still strictly better than master (master overflowed 3 rows across h=30..39; this overflows 1-4 only at h=31..35, fits from h=36), so it ships — but the dialog.go:3860-3880 comment must stop asserting the sum is the constraint.
Triage of 6 deferred minors: #4 FIX before merge (stale index comment); #1,#2,#3,#6 leave; #5 leave (see correction above).
ONE consolidated fix wave dispatched (skill: one wave, then one scoped re-review).
FINAL FIX WAVE: commits 89382dd (gitworktree error classification) + 169b032 (render/request guards, comment correction, render tests). All 7 items applied.
Implementer caught a FALSE GREEN in its own item-6 test via the required mutation check: worktreePickModel's zero-value height clamped worktreeVisibleRows() to the 1-row floor, so the row carrying the control-character branch name never rendered — the test passed with sanitizeRemoteText REMOVED. Fixed the model height to 40. Without the mandated remove-and-confirm-red step this would have shipped a test asserting nothing.
CONTROLLER VERIFIED THE MUTATION INDEPENDENTLY: removed the sanitizeRemoteText call at dialog.go:3768, ran the suite — TestRenderSetup_WorktreeRowsSanitizeRemoteText fails with "rendered output carries a raw CSI sequence from a remote branch name", and only that test. Restored byte-identically (git diff clean). The trust boundary is genuinely pinned.
FIX-WAVE SCOPED RE-REVIEW: all 7 items ADDRESSED, no new breakage, no scope creep, both commits compliant. Recommend merge.
Re-reviewer independently verified the two flagged risks: (1) item 3's arithmetic is byte-identical to 6e6d633, only comment lines in the hunk, and the single-expander premise was re-derived from renderCreatePaneSetupDialog rather than taken from me; (2) item 5 does NOT regress ordinary non-repo directories — List's plain-non-zero-exit still collapses to (nil,nil), so worktreeListResponse still yields Repo=false/Error="".
Noted, not blocking: if the user tabs into the worktree field before the async CWD resolution lands, the field parks in "never asked" and only re-fetches on the NEXT focus event. Identical to the already-shipped session field behaviour (sessions.go:111-119), so it is existing convention rather than a regression.
BRANCH COMPLETE: 14 commits, 27 packages green, vet clean, docs-size clean. NOT pushed, no PR opened — awaiting user decision.
POST-COMPLETION: PR #132 merged to master (48b948b), then release v1.50.0 (d15a4c0) rotated [Unreleased] into a dated section — the cause of the predicted CHANGELOG conflict. Rebased feature/worktree-panes-stage-a onto the new master; resolved by placing stage A's entry under the fresh [Unreleased] with the 1.50.0 section preserved intact. Post-rebase: 14 commits, 0 behind master, 27 packages green, vet clean, `git merge-tree` reports a CLEAN MERGE (zero conflicts). Branch unpushed, awaiting user decision on opening its PR.
